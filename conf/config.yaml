# null -> resolved to ~/experiments-gnn-gap (see commander.py). Override on the CLI
# with `root_dir=/path/to/data` to point at an existing data/checkpoint tree.
root_dir: null

defaults:
  - _self_
  - dataset: sparse
  - model: fgnn
  - training: gpu
  - pipeline: chain_noseed
  - hydra/run: default
